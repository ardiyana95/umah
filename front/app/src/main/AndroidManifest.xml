<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.umah_app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".result.DetailPage" android:label="@string/title_detail_page" />
        <activity
            android:name=".search.SortKriteria"
            android:label="@string/title_kriteria" />
        <activity
            android:name=".result.Output"
            android:label="@string/title_output" />
        <activity
            android:name=".result.tabbed"
            android:label="@string/title_activity_tabbed" />
        <activity
            android:name=".search.FilterKriteria"
            android:label="@string/title_search" />
        <activity
            android:name=".search.Person"
            android:label="@string/title_activity_person" />
        <activity android:name=".onboard.OnBoard" />
        <activity android:name=".home.Home" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>